# Cloned reference repos (tracked in their own upstreams, not here)
/llm-app-lab/
/project_management_with_ai_agent/
/quali-fit/

# Private meeting transcripts (never commit raw notes)
/meetings/raw/*
!/meetings/raw/.gitkeep

# Python
__pycache__/
*.pyc

# OS / editor
.DS_Store
Thumbs.db

# Phase 5 로컬 override (프로젝트별 설정, 커밋 안 함)
.harness/

# Cloudflare 로컬 dev 상태(루트에서 wrangler dev -c 실행 시 생성)
/.wrangler/
